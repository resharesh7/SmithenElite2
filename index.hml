<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Smithen Elite Soft Washing LLC</title>
  <meta name="description" content="Top-rated pressure washing and soft washing services for homeowners in the USA. Book now with Smithen Elite Soft Washing LLC.">
  <meta name="keywords" content="Pressure Washing, Soft Washing, House Washing, Exterior Cleaning, Power Washing, Roof Cleaning, Driveway Washing, USA Cleaning Services">
  <meta name="author" content="Smithen Elite Soft Washing LLC">
  <style>
    :root {
      --blue: #0a2239;
      --white: #ffffff;
      --red: #b22222;
    }

    body, html {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      background: var(--white);
      color: var(--blue);
      overflow-x: hidden;
    }

    .loading-screen {
      background-color: var(--blue);
      color: white;
      font-size: 2rem;
      text-align: center;
      padding-top: 40vh;
      height: 100vh;
      width: 100vw;
      position: fixed;
      z-index: 9999;
      top: 0;
      left: 0;
    }

    header {
      background: var(--red);
      color: var(--white);
      padding: 1rem 2rem;
      text-align: center;
    }

    nav ul {
      list-style: none;
      padding: 0;
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
    }

    nav li {
      margin: 0 1rem;
    }

    nav a {
      color: var(--white);
      text-decoration: none;
      font-weight: bold;
    }

    .hero {
      text-align: center;
      padding: 4rem 2rem;
      background: var(--blue);
      color: var(--white);
    }

    .btn-primary {
      background: var(--red);
      color: white;
      padding: 0.75rem 1.5rem;
      border: none;
      border-radius: 10px;
      text-decoration: none;
      font-weight: bold;
    }

    section {
      padding: 2rem;
    }

    .services-list {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1rem;
      list-style: none;
      padding: 0;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      max-width: 400px;
      margin: auto;
    }

    form input, form textarea, form button {
      padding: 0.75rem;
      font-size: 1rem;
    }

    footer {
      background: var(--blue);
      color: white;
      text-align: center;
      padding: 1rem;
    }

    canvas#rainCanvas {
      position: fixed;
      top: 0;
      left: 0;
      pointer-events: none;
      width: 100vw;
      height: 100vh;
      z-index: 1;
    }

    .music-toggle {
      position: fixed;
      bottom: 15px;
      left: 15px;
      background: var(--red);
      color: white;
      border: none;
      border-radius: 50px;
      padding: 0.75rem 1.5rem;
      z-index: 10;
      cursor: pointer;
    }

    @media (max-width: 600px) {
      nav ul {
        flex-direction: column;
      }

      .music-toggle {
        font-size: 0.9rem;
        padding: 0.5rem 1rem;
      }
    }
  </style>
</head>
<body>
  <div id="loader" class="loading-screen">🇺🇸 Loading Smithen Elite...</div>

  <canvas id="rainCanvas"></canvas>
  <audio id="rainAudio" loop>
    <source src="https://cdn.pixabay.com/download/audio/2022/08/31/audio_c8827fd9a5.mp3?filename=soft-rain-loop-ambient-116199.mp3" type="audio/mpeg">
  </audio>
  <button class="music-toggle" id="musicBtn">🔊 Rain Sounds</button>

  <header>
    <h1>Smithen Elite Soft Washing LLC 🇺🇸</h1>
    <nav>
      <ul>
        <li><a href="#services">Services</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
    </nav>
  </header>

  <main>
    <section class="hero">
      <h2>Your Home Deserves Elite Care</h2>
      <p>Specializing in soft washing & pressure washing for homeowners who demand results.</p>
      <a href="#contact" class="btn-primary">Get a Free Quote</a>
    </section>

    <section id="services">
      <h2>Our Services</h2>
      <ul class="services-list">
        <li>🏡 House Washing</li>
        <li>🧼 Roof Cleaning</li>
        <li>🧽 Driveway & Sidewalk Cleaning</li>
        <li>🪟 Window & Gutter Cleaning</li>
      </ul>
    </section>

    <section id="about">
      <h2>About Us</h2>
      <p>Veteran-owned and proudly serving our American community 🇺🇸. Smithen Elite delivers high-quality, eco-friendly soft washing and pressure washing.</p>
    </section>

    <section id="contact">
      <h2>Contact Us</h2>
      <form>
        <input type="text" placeholder="Your Name" required>
        <input type="email" placeholder="Email" required>
        <textarea placeholder="Your Message"></textarea>
        <button type="submit">Send</button>
      </form>
    </section>
  </main>

  <footer>
    <p>&copy; 2025 Smithen Elite Soft Washing LLC | Serving with pride 🇺🇸</p>
  </footer>

  <script>
    // Loader
    window.addEventListener("load", () => {
      document.getElementById("loader").style.display = "none";
    });

    // Music toggle
    const musicBtn = document.getElementById("musicBtn");
    const rainAudio = document.getElementById("rainAudio");

    let isPlaying = false;
    musicBtn.addEventListener("click", () => {
      if (isPlaying) {
        rainAudio.pause();
        musicBtn.innerText = "🔇 Rain Sounds";
      } else {
        rainAudio.play();
        musicBtn.innerText = "🔊 Rain Sounds";
      }
      isPlaying = !isPlaying;
    });

    // Rain animation
    const canvas = document.getElementById("rainCanvas");
    const ctx = canvas.getContext("2d");
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;

    let rainDrops = [];

    function createRain() {
      for (let i = 0; i < 300; i++) {
        rainDrops.push({
          x: Math.random() * canvas.width,
          y: Math.random() * canvas.height,
          length: Math.random() * 15 + 5,
          speed: Math.random() * 3 + 2
        });
      }
    }

    function drawRain() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      ctx.strokeStyle = "rgba(173,216,230,0.5)";
      ctx.lineWidth = 1;

      rainDrops.forEach(drop => {
        ctx.beginPath();
        ctx.moveTo(drop.x, drop.y);
        ctx.lineTo(drop.x, drop.y + drop.length);
        ctx.stroke();

        drop.y += drop.speed;
        if (drop.y > canvas.height) {
          drop.y = -drop.length;
        }
      });

      requestAnimationFrame(drawRain);
    }

    createRain();
    drawRain();
  </script>
</body>
</html>
